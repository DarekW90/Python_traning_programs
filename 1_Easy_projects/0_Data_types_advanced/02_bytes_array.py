# Tablice bajtowe - bytearray - sąpodobne do bajtów ale są mutowalne, co oznacza, że można je modyfikować po utworzeniu.
# przykład:
#     bajt_array = bytearray(b'hello')

bajt_array = bytearray(b'hello')
print(bajt_array[0])
